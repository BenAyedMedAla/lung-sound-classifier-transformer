# Hugging Face cache on D: to avoid C: disk space issues
HF_HOME=D:/hf_cache
HUGGINGFACE_HUB_CACHE=D:/hf_cache/hub
TRANSFORMERS_CACHE=D:/hf_cache/transformers

# Optional token for faster / higher-rate HF access
HF_TOKEN=

# Preprocess temporary files (memmap) on D:
ICBHI_PREPROCESS_TMP=..
